# Supabase (server-side only — NUNCA expor ao browser)
SUPABASE_URL=https://....supabase.co
SUPABASE_KEY=

# URL pública do site (exposta ao browser para URLs canônicas e share)
NEXT_PUBLIC_SITE_URL=https://trumptracker.workers.dev

# Token para revalidação ISR (POST /api/revalidate)
# Gerar com: openssl rand -hex 32
REVALIDATE_SECRET=


CLOUDFLARE_API_TOKEN=

CLOUDFLARE_ACCOUNT_ID=