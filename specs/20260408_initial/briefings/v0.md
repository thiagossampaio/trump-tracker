# Trump Tracker — Especificação Técnica Completa

> **Versão:** 1.0.0  
> **Status:** Em desenvolvimento — aguardando retomada  
> **Última atualização:** 2026-04-08  
> **Propósito deste arquivo:** Especificação completa para retomada do desenvolvimento em nova sessão. Contém toda a arquitetura, decisões de design, código já desenvolvido e pendências.

---

## Índice

1. [Visão Geral do Produto](#1-visão-geral-do-produto)
2. [Stack Tecnológico](#2-stack-tecnológico)
3. [Arquitetura do Sistema](#3-arquitetura-do-sistema)
4. [Schema do Banco de Dados](#4-schema-do-banco-de-dados)
5. [Pipeline Autônomo](#5-pipeline-autônomo)
6. [Código Desenvolvido](#6-código-desenvolvido)
7. [Estrutura de Documentação Mintlify](#7-estrutura-de-documentação-mintlify)
8. [Boas Práticas e Regras de Desenvolvimento](#8-boas-práticas-e-regras-de-desenvolvimento)
9. [Critérios de Aceite](#9-critérios-de-aceite)
10. [Roadmap Atualizado](#10-roadmap-atualizado)
11. [Variáveis de Ambiente](#11-variáveis-de-ambiente)
12. [Estrutura de Repositório](#12-estrutura-de-repositório)
13. [Pendências e Próximos Passos](#13-pendências-e-próximos-passos)

---

## 1. Visão Geral do Produto

### O que é

Um site público que compila, de forma autônoma e contínua, eventos aberrantes, inusitados ou sem precedente histórico da presidência Donald Trump. O feed é apresentado em scroll infinito, ordem cronológica reversa (mais recente primeiro), com linguagem factual, fontes verificáveis e um **Aberration Score** de 1 a 10.

### O que não é

- Não é um site de opinião
- Não é um agregador genérico de notícias
- Não é um site de sátira ou humor
- Não requer curadoria manual contínua — apenas moderação pontual de eventos de alto impacto via Telegram

### Propósito editorial

Medir e arquivar o **desvio histórico da norma presidencial americana**. O Aberration Score quantifica o quão inusitado é cada evento com base em critérios objetivos: precedente histórico, velocidade/reversão de posição, impacto institucional e reação do sistema político (judiciário, mercados, aliados).

### Potencial viral

Cada evento tem URL própria com OG image gerada dinamicamente — otimizada para compartilhamento em Twitter/X e WhatsApp. O contador no header ("X aberrações desde a posse") atualiza em tempo real e funciona como gancho de engajamento.

---

## 2. Stack Tecnológico

| Camada | Tecnologia | Justificativa |
|---|---|---|
| **Frontend** | Next.js 15 (App Router) + TailwindCSS + shadcn/ui | ISR nativo, OG images via `@vercel/og`, edge runtime compatível com Cloudflare |
| **Hosting** | Cloudflare Pages | CDN global gratuita, deploy via Git push, Workers para webhook do Telegram |
| **Banco de dados** | Supabase (Postgres + pgvector) | Vetores nativos para dedup semântico, free tier robusto, realtime, Row Level Security |
| **IA — Classificação** | Claude claude-sonnet-4-20250514 (Anthropic) | Superior em raciocínio histórico e distinção fato/interpretação vs. GPT-4o |
| **IA — Embeddings** | `text-embedding-3-small` (OpenAI) | Melhor custo-eficiência para dedup semântico (1536 dims, $0.00002/1k tokens) |
| **Orquestração** | GitHub Actions (cron) | Gratuito para repos públicos, 2000 min/mês free, secrets nativos |
| **Moderação** | Telegram Bot API + Cloudflare Worker (webhook) | Interface móvel, zero infra adicional, inline keyboards, zero custo de compute |
| **Fontes primárias** | AP RSS, Reuters RSS | Tier 1, sem API key, máxima confiabilidade |
| **Fontes secundárias** | GDELT, The Guardian API, NewsAPI | GDELT gratuito e ilimitado; Guardian e NewsAPI com free tier generoso |
| **Documentação** | Mintlify | Padrão de mercado para projetos técnicos open source, suporte MDX nativo |

### Custo estimado mensal em produção

| Serviço | Custo |
|---|---|
| Cloudflare Pages | $0 |
| Supabase (free tier — 500MB, 50k req/dia) | $0 |
| GitHub Actions (repo público) | $0 |
| AP RSS + Reuters RSS | $0 |
| GDELT API | $0 |
| Guardian API (free developer key) | $0 |
| NewsAPI (free: 100 req/dia) | $0 |
| Claude API (~20 eventos úteis/dia × $0.002) | ~$1.20 |
| OpenAI Embeddings (~20 embeddings/dia) | ~$0.10 |
| **Total** | **~$1.30/mês** |

---

## 3. Arquitetura do Sistema

### Decisão crítica de design

**Não há arquivos `.jsonl` ou arquivos temporários em disco no pipeline de produção.** Esta foi uma correção explícita feita durante o desenvolvimento: o padrão de arquivos locais funciona para pipelines CLI (como o projeto `career-ops` que serviu de referência), mas é inadequado para um sistema com cron rodando a cada 2h no GitHub Actions, pois:

- GitHub Actions não tem filesystem persistente entre runs
- Dois crons simultâneos escrevem no mesmo arquivo → corrupção
- Arquivos crescem indefinidamente sem garbage collection
- Nenhum índice, nenhuma query, nenhum rollback

**Toda persistência é no Supabase desde o primeiro byte ingerido.**

### Fluxo completo em produção

```
┌──────────────────────────────────────────────────────────────┐
│                  GITHUB ACTIONS (cron a cada 2h)             │
│                  + workflow_dispatch (manual)                 │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
            ┌────────────────────────────┐
            │      ingest_agent.py       │
            │                            │
            │  Busca em PARALELO:        │
            │  • AP RSS (Tier 1)         │
            │  • Reuters RSS (Tier 1)    │
            │  • GDELT (Tier 2)          │
            │  • Guardian API (Tier 2)   │
            │  • NewsAPI (Tier 2)        │
            │                            │
            │  Filtro keyword no título  │
            │  (sem IA, sem custo)       │
            │                            │
            │  Dedup por URL em batch    │
            │  contra raw_articles       │
            └────────────┬───────────────┘
                         │
                         │ INSERT raw_articles
                         │ status = 'pending'
                         ▼
            ┌─────────────────────────────────────────────────┐
            │                   SUPABASE                      │
            │                                                 │
            │   tabela: raw_articles    tabela: events        │
            │   ───────────────────     ─────────────         │
            │   id, url, title, body,   (feed público —       │
            │   source_name, tier,       preenchida pelo       │
            │   published_at,            publish_agent)        │
            │   status: pending /                             │
            │           classified /                          │
            │           rejected /                            │
            │           published                             │
            └────────────┬────────────────────────────────────┘
                         │
                         │ SELECT WHERE status = 'pending'
                         │ ORDER BY priority DESC, published_at DESC
                         ▼
            ┌────────────────────────────┐
            │     classify_agent.py      │
            │                            │
            │  Lê batches de 5 artigos   │
            │  por chamada (economia     │
            │  de tokens)                │
            │                            │
            │  Claude Sonnet:            │
            │  • is_aberrant (bool)      │
            │  • score (1–10)            │
            │  • score_breakdown (4 dim) │
            │  • category                │
            │  • headline_pt             │
            │  • summary_pt              │
            │  • historical_context      │
            │  • confidence              │
            │  • needs_human_review      │
            │                            │
            │  UPDATE status =           │
            │  'classified' | 'rejected' │
            └────────────┬───────────────┘
                         │
            ┌────────────┴────────────┐
            ▼                         ▼
       score 4–7                  score ≥ 8
       (auto-approve)             (human review)
            │                         │
            ▼                         ▼
   dedup_agent.py           telegram_agent.py
                                       │
   pgvector similarity        Envia card para
   cosine threshold:          TELEGRAM_CHAT_ID:
   ≥0.92 → duplicata          • Headline
   0.80–0.91 → update         • Summary
   0.65–0.79 → relacionado    • Score breakdown
   <0.65 → independente       • Link fonte
                              • [✅ Pub] [✏️ Edit] [❌ Rej]
            │                         │
            ▼                Cloudflare Worker
   publish_agent.py          recebe callback
            │                do inline keyboard
            │                         │
            └──────────┬──────────────┘
                       │
                       ▼
            ┌────────────────────────────┐
            │     publish_agent.py       │
            │                            │
            │  Gera embedding final      │
            │  Gera slug único           │
            │  INSERT events             │
            │  UPDATE raw_articles       │
            │  status = 'published'      │
            │  POST /api/revalidate      │
            │  (revalida cache Next.js)  │
            └────────────┬───────────────┘
                         │
                         ▼
              Feed público 🌐
              Cloudflare Pages
              (ISR — servido do CDN)
```

### Ciclo de vida de um artigo

```
raw_articles.status

'pending'     → recém inserido pelo ingest_agent, aguarda classify
'classified'  → IA processou; se is_aberrant=true vai para dedup/telegram
'rejected'    → não passou nos critérios (score ≤ 3, rotineiro, duplicata)
'published'   → publicado no feed (existe entrada correspondente em events)
```

---

## 4. Schema do Banco de Dados

```sql
-- supabase/schema.sql
-- Execute no Supabase SQL Editor antes do primeiro run

CREATE EXTENSION IF NOT EXISTS vector;

-- ──────────────────────────────────────────
-- Inbox de tudo que foi ingerido (nunca apagar)
-- ──────────────────────────────────────────
CREATE TABLE IF NOT EXISTS raw_articles (
    id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),

    -- Identificação e dedup
    url          TEXT UNIQUE NOT NULL,
    url_hash     TEXT NOT NULL,         -- sha256[:16] para dedup rápido

    -- Conteúdo
    title        TEXT NOT NULL,
    body         TEXT,                  -- primeiros 2000 chars

    -- Origem
    source_name  TEXT NOT NULL,
    source_tier  SMALLINT DEFAULT 2,    -- 1=primária, 2=referência, 3=suplementar
    raw_query    TEXT,                  -- qual query gerou este artigo

    -- Temporal
    published_at TIMESTAMPTZ NOT NULL,
    fetched_at   TIMESTAMPTZ DEFAULT NOW(),

    -- Pipeline
    status       TEXT DEFAULT 'pending'
                 CHECK (status IN ('pending','classified','rejected','published')),
    priority     BOOLEAN DEFAULT FALSE, -- keywords de alta prioridade no título
    processed_at TIMESTAMPTZ            -- quando o classify_agent processou
);

CREATE INDEX IF NOT EXISTS idx_raw_articles_status
    ON raw_articles (status, priority DESC, published_at DESC);
CREATE INDEX IF NOT EXISTS idx_raw_articles_published
    ON raw_articles (published_at DESC);
CREATE INDEX IF NOT EXISTS idx_raw_articles_url_hash
    ON raw_articles (url_hash);

-- ──────────────────────────────────────────
-- Feed público (preenchida pelo publish_agent)
-- ──────────────────────────────────────────
CREATE TABLE IF NOT EXISTS events (
    id                UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    slug              TEXT UNIQUE NOT NULL,

    -- Conteúdo editorial (pt-BR)
    headline          TEXT NOT NULL,
    summary           TEXT NOT NULL,
    historical_context TEXT,
    tags              JSONB DEFAULT '[]',

    -- Classificação
    score             SMALLINT NOT NULL CHECK (score BETWEEN 1 AND 10),
    score_breakdown   JSONB,            -- {precedent, velocity, inst_impact, system_reaction}
    category          TEXT NOT NULL,
    confidence        TEXT DEFAULT 'high' CHECK (confidence IN ('high','medium','low')),

    -- Fontes
    source_url        TEXT NOT NULL,
    source_name       TEXT NOT NULL,
    source_tier       SMALLINT DEFAULT 2,
    secondary_sources JSONB DEFAULT '[]',

    -- Temporal
    occurred_at       TIMESTAMPTZ NOT NULL,
    created_at        TIMESTAMPTZ DEFAULT NOW(),
    updated_at        TIMESTAMPTZ DEFAULT NOW(),

    -- Engajamento
    share_count       INT DEFAULT 0,
    view_count        INT DEFAULT 0,

    -- Pipeline / revisão
    review_status     TEXT DEFAULT 'auto'
                      CHECK (review_status IN ('auto','human_approved','human_edited')),
    reviewed_by       TEXT,
    merged_into_id    UUID REFERENCES events(id),
    raw_article_id    UUID REFERENCES raw_articles(id),

    -- Semântico
    embedding         vector(1536)
);

CREATE INDEX IF NOT EXISTS idx_events_occurred_at
    ON events (occurred_at DESC);
CREATE INDEX IF NOT EXISTS idx_events_category
    ON events (category);
CREATE INDEX IF NOT EXISTS idx_events_score
    ON events (score DESC);
CREATE INDEX IF NOT EXISTS idx_events_embedding
    ON events USING ivfflat (embedding vector_cosine_ops)
    WITH (lists = 100);

-- View para o feed público (exclui mesclados/removidos)
CREATE OR REPLACE VIEW public_feed AS
SELECT id, slug, headline, summary, score, category,
       source_url, source_name, secondary_sources,
       occurred_at, historical_context, tags,
       share_count, view_count, review_status
FROM events
WHERE merged_into_id IS NULL
ORDER BY occurred_at DESC;
```

---

## 5. Pipeline Autônomo

### GitHub Actions — workflow completo

```yaml
# .github/workflows/pipeline.yml
name: Trump Tracker — Autonomous Pipeline

on:
  schedule:
    - cron: '0 */2 * * *'    # a cada 2 horas
  workflow_dispatch:           # disparo manual com inputs opcionais
    inputs:
      lookback_hours:
        description: 'Horas de lookback para ingestão'
        required: false
        default: '4'
      dry_run:
        description: 'Dry run (sem gravar no banco)'
        type: boolean
        required: false
        default: false

jobs:
  pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run ingest_agent
        run: |
          python agents/ingest_agent.py \
            --lookback-hours ${{ github.event.inputs.lookback_hours || '4' }} \
            ${{ github.event.inputs.dry_run == 'true' && '--dry-run' || '' }}

      - name: Run classify_agent
        run: python agents/classify_agent.py

      - name: Run dedup_agent
        run: python agents/dedup_agent.py

      - name: Run publish_agent
        run: python agents/publish_agent.py

    env:
      SUPABASE_URL:         ${{ secrets.SUPABASE_URL }}
      SUPABASE_KEY:         ${{ secrets.SUPABASE_KEY }}
      ANTHROPIC_API_KEY:    ${{ secrets.ANTHROPIC_API_KEY }}
      OPENAI_API_KEY:       ${{ secrets.OPENAI_API_KEY }}
      NEWSAPI_KEY:          ${{ secrets.NEWSAPI_KEY }}
      GUARDIAN_API_KEY:     ${{ secrets.GUARDIAN_API_KEY }}
      TELEGRAM_BOT_TOKEN:   ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID:     ${{ secrets.TELEGRAM_CHAT_ID }}
      REVALIDATE_SECRET:    ${{ secrets.REVALIDATE_SECRET }}
      NEXT_PUBLIC_SITE_URL: ${{ secrets.NEXT_PUBLIC_SITE_URL }}
```

### Moderação via Telegram — fluxo detalhado

```
classify_agent detecta score ≥ 8 ou confidence = 'low'
        │
        ▼
telegram_agent.py constrói mensagem:

┌─────────────────────────────────────────────────────┐
│ 🔴 REVISÃO NECESSÁRIA — Score 9/10                  │
│                                                     │
│ 📰 Trump dissolve comissão eleitoral                │
│    independente por ordem executiva                 │
│                                                     │
│ 📋 RESUMO:                                          │
│ Presidente extinguiu a EAC, criada por lei do       │
│ Congresso em 2002. Sem precedente histórico         │
│ documentado. Três ex-presidentes republicanos       │
│ emitiram declaração conjunta criticando a medida.   │
│                                                     │
│ 📊 BREAKDOWN:                                       │
│ Precedente: 4/4 · Velocidade: 2/2                   │
│ Institucional: 2/2 · Reação: 1/2                    │
│                                                     │
│ 🔗 Fonte: Reuters                                   │
│ https://reuters.com/...                             │
│                                                     │
│ [✅ Publicar]  [✏️ Editar]  [❌ Rejeitar]           │
└─────────────────────────────────────────────────────┘

Moderador responde via inline keyboard
        │
        ▼
Cloudflare Worker (webhook) recebe callback
        │
   ┌────┴──────┬──────────┐
   ▼           ▼          ▼
Publicar    Editar     Rejeitar
   │           │          │
   │       Bot pede    UPDATE status
   │       nova        = 'rejected'
   │       headline/
   │       summary
   │           │
   └─────┬─────┘
         ▼
  UPDATE status = 'approved'
  → publish_agent processa
    no próximo run do cron
```

**Webhook Handler (Cloudflare Worker):**

O handler não é um servidor — é um Worker serverless que acorda apenas quando o moderador responde. Zero custo de compute em idle.

```javascript
// cloudflare/telegram-webhook.js
export default {
  async fetch(request, env) {
    if (request.method !== 'POST') {
      return new Response('Method not allowed', { status: 405 });
    }

    const update = await request.json();

    // Callback de inline keyboard
    if (update.callback_query) {
      const { data, message } = update.callback_query;
      const [action, articleId] = data.split(':');

      const supabaseUrl = env.SUPABASE_URL;
      const supabaseKey = env.SUPABASE_KEY;

      if (action === 'publish') {
        await fetch(`${supabaseUrl}/rest/v1/raw_articles?id=eq.${articleId}`, {
          method: 'PATCH',
          headers: {
            'apikey': supabaseKey,
            'Authorization': `Bearer ${supabaseKey}`,
            'Content-Type': 'application/json',
          },
          body: JSON.stringify({ status: 'approved_manual' }),
        });

        // Confirma para o Telegram
        await answerCallback(update.callback_query.id, '✅ Aprovado! Será publicado no próximo run.', env);

      } else if (action === 'reject') {
        await fetch(`${supabaseUrl}/rest/v1/raw_articles?id=eq.${articleId}`, {
          method: 'PATCH',
          headers: {
            'apikey': supabaseKey,
            'Authorization': `Bearer ${supabaseKey}`,
            'Content-Type': 'application/json',
          },
          body: JSON.stringify({ status: 'rejected' }),
        });

        await answerCallback(update.callback_query.id, '❌ Rejeitado.', env);
      }
    }

    return new Response('OK', { status: 200 });
  }
};

async function answerCallback(callbackQueryId, text, env) {
  await fetch(`https://api.telegram.org/bot${env.TELEGRAM_BOT_TOKEN}/answerCallbackQuery`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ callback_query_id: callbackQueryId, text }),
  });
}
```

---

## 6. Código Desenvolvido

### 6.1 `CLAUDE.md`

```markdown
# Trump Tracker — AI Aberration Feed

## O que é isso

Pipeline de inteligência artificial que monitora, classifica e publica automaticamente
eventos inusitados, aberrantes ou sem precedentes históricos envolvendo Donald Trump.

O resultado é um feed web em scroll infinito — reverse-chronological — onde cada card
tem headline impactante, categoria, Aberration Score (1–10) e botão de share com OG
image dinâmica.

Não é um site de opinião. É um arquivo factual, baseado em fontes verificáveis,
com IA avaliando o grau de inusitado de cada evento em relação à norma histórica
da presidência americana.

---

## First Run — Onboarding

Antes de qualquer coisa, verifique silenciosamente se o sistema está configurado.
Execute esses checks toda vez que uma sessão começar:

1. Existe `.env` com as API keys necessárias?
2. Existe `config/sources.yml`?
3. O banco de dados Supabase está acessível?

Se algum desses estiver faltando, entre em modo onboarding.

### Passo 1: API Keys (obrigatório)

Se `.env` estiver faltando ou incompleto, pergunte:

> "Preciso das seguintes chaves para o sistema funcionar:
> - NEWSAPI_KEY — newsapi.org (free tier)
> - GUARDIAN_API_KEY — open-platform.theguardian.com (free)
> - ANTHROPIC_API_KEY — classificação inteligente
> - SUPABASE_URL + SUPABASE_KEY — banco de dados
> - TELEGRAM_BOT_TOKEN + TELEGRAM_CHAT_ID — moderação
> - OPENAI_API_KEY — embeddings para dedup
> Quais você já tem?"

### Passo 2: Fontes

Copie `config/sources.example.yml` → `config/sources.yml`.

### Passo 3: Banco de dados

Execute `supabase/schema.sql` no Supabase SQL Editor.

### Passo 4: Pronto

> "Sistema configurado! Comandos disponíveis:
> - /tracker ingest — buscar eventos das últimas 2h
> - /tracker classify — classificar pendentes
> - /tracker publish — publicar aprovados
> - /tracker status — painel de métricas
> - /tracker backfill [dias] — histórico"

---

## Skill Modes

| Se o usuário...              | Modo       |
|------------------------------|------------|
| Pede /tracker ingest         | ingest     |
| Pede /tracker classify       | classify   |
| Pede /tracker publish        | publish    |
| Pede /tracker status         | status     |
| Pede /tracker backfill       | backfill   |
| Cola URL ou texto de notícia | auto-pipeline |

---

## Princípios editoriais — CRÍTICOS

Este sistema classifica, não opina. O Aberration Score mede desvio histórico
da norma presidencial americana — não aprovação ou desaprovação política.

### Regras de publicação

1. NUNCA publique sem pelo menos 1 fonte verificável Tier 1 ou 2
2. SEMPRE link para a fonte primária — não o agregador
3. Separe fato de interpretação. A headline descreve o que aconteceu
4. Human-in-the-loop OBRIGATÓRIO para score ≥ 8 via Telegram
5. Dedup obrigatório. Mesmo evento com múltiplas coberturas = 1 card

---

## Convenções técnicas

- Python 3.12, asyncio, httpx (async), Pydantic v2
- Supabase (Postgres + pgvector) — toda persistência
- Next.js 15 App Router, TailwindCSS, shadcn/ui
- GitHub Actions para cron (a cada 2h)
- Cloudflare Pages para frontend + Worker para webhook Telegram
- NUNCA arquivos .jsonl ou .tsv em produção — tudo no banco
```

### 6.2 `.claude/skills/trump-tracker/_shared.md`

```markdown
# Shared Context — Trump Tracker

## Categorias canônicas

| Categoria      | O que cobre                                              | Emoji |
|----------------|----------------------------------------------------------|-------|
| Institucional  | Ataques a checks and balances, demissões de independentes | 🏛️   |
| Econômico      | Tarifas, política comercial, sanções, mercados           | 📈    |
| Diplomático    | Relações internacionais, OTAN, alianças, tratados        | 🌐    |
| Jurídico       | Processos, indultos, obstrução, privilégios executivos   | ⚖️    |
| Militar        | Forças armadas, estado de emergência, cadeia de comando  | 🎖️   |
| Social         | Direitos civis, imigração, minorias, saúde pública       | 👥    |
| Comunicação    | Declarações falsas documentadas, ataques à imprensa      | 📢    |

---

## Rubrica do Aberration Score (1–10)

Score = soma de 4 dimensões:

**A. Precedente histórico (0–4)**
- 0: Aconteceu antes, múltiplas vezes, nos últimos 50 anos
- 1: Aconteceu antes, mas há mais de 50 anos
- 2: Aconteceu uma vez, em circunstâncias muito diferentes
- 3: Sem precedente direto, análogos parciais existem
- 4: Absolutamente sem precedente na história da república americana

**B. Velocidade / escalada (0–2)**
- 0: Decisão gradual com sinalizações anteriores
- 1: Decisão abrupta sem aviso
- 2: Reverteu ou escalou em 48h de declaração anterior do próprio Trump

**C. Impacto institucional (0–2)**
- 0: Impacto apenas na política pública (reversível)
- 1: Impacto em normas não-escritas (erosão de precedente)
- 2: Impacto em estruturas constitucionais ou legais

**D. Reação do sistema (0–2)**
- 0: Reação normal de oposição partidária
- 1: Reação bipartidária ou de instituições neutras
- 2: Reação de aliados, militares, judiciário federal ou mercados

### Tabela de referência

| Score | Classificação             |
|-------|---------------------------|
| 1–3   | Normal presidencial       |
| 4–5   | Incomum                   |
| 6–7   | Raro                      |
| 8–9   | Sem precedente recente    |
| 10    | Sem precedente histórico  |

---

## Fontes por tier

**Tier 1:** Reuters, AP, AFP, C-SPAN, documentos oficiais
**Tier 2:** NYT, WaPo, WSJ, BBC, Guardian, FT, ProPublica, Politico
**Tier 3:** Agregadores, blogs, redes sociais — NUNCA fonte primária

---

## Padrão de headline (pt-BR)

- Máximo 15 palavras
- Tempo verbal: passado simples
- Sem adjetivos avaliativos
- Incluir contraste quando existir

✅ "Trump demite diretor do FBI dois dias após negar que o faria"
❌ "Trump volta a atacar democracia ao demitir diretor do FBI"

---

## Padrão de summary (pt-BR)

3 frases máximo:
1. O que aconteceu (fato)
2. Por que é incomum (contexto histórico)
3. Reação imediata se relevante
```

### 6.3 `.claude/skills/trump-tracker/ingest.md`

```markdown
# Skill: ingest

Busca eventos nas fontes configuradas, normaliza o formato e persiste
na tabela raw_articles com status='pending'.

## Quando usar
- /tracker ingest
- Chamado pelo GitHub Actions a cada 2h

## O que fazer

1. Ler config/sources.yml (queries, fontes ativas, lookback_hours)
2. Buscar em paralelo: NewsAPI, GDELT, Guardian, AP RSS, Reuters RSS
3. Filtro pré-IA por keyword no título
4. Dedup por URL em batch contra raw_articles
5. INSERT com status='pending', priority baseada em HIGH_PRIORITY_TERMS
6. Imprimir relatório

## Filtros de relevância (sem IA)

RELEVANCE_TERMS = ["trump", "white house", "executive order",
                   "president trump", "trump administration"]

HIGH_PRIORITY_TERMS = ["unprecedented", "first time", "fires",
                       "fired", "emergency", "pardon", "reversed",
                       "invokes", "suspended", "never before"]

## Relatório final
Fontes consultadas, artigos brutos, após filtro, dedup, inseridos.
Próximo passo: classify_agent.py
```

### 6.4 `.claude/skills/trump-tracker/classify.md`

```markdown
# Skill: classify

Lê raw_articles com status='pending', chama Claude em batches de 5,
produz score + headline + summary, atualiza status no banco.

## Estratégias de economia de tokens

1. Filtro pré-IA no ingest reduz ~85% do volume
2. Truncagem do corpo: apenas 800 chars por artigo
3. Batching: 5 artigos por chamada (70% menos overhead de system prompt)
4. Prompt Caching da Anthropic: system prompt cacheado por 5 min
5. Resposta JSON direta sem markdown

## Roteamento por score

- score ≤ 3: UPDATE status='rejected'
- score 4–7: vai para dedup_agent
- score ≥ 8: vai para telegram_agent (moderação humana)
- confidence='low': sempre vai para moderação independente do score

## Custo estimado
~$0.002 por evento classificado = ~$1.20/mês (20 eventos/dia)
```

### 6.5 `.claude/skills/trump-tracker/dedup.md`

```markdown
# Skill: dedup

Detecta eventos duplicados usando pgvector + matching de data/categoria.

## Embedding
- Modelo: text-embedding-3-small (1536 dims)
- Texto: "{headline} {summary} {category}"

## Tabela de decisão por cosine similarity

| Similaridade | Ação                                                |
|--------------|-----------------------------------------------------|
| ≥ 0.92       | Duplicata exata → descartar                         |
| 0.80–0.91    | Mesmo episódio → verificar se é update              |
| 0.65–0.79    | Relacionado → publicar separado, linkar             |
| < 0.65       | Independente → publicar normalmente                 |

## Update vs. Duplicata (similaridade 0.80–0.91)

É UPDATE se:
- occurred_at do novo > existente em mais de 2h
- Adiciona informação nova (keywords: blocks, responds, reverses)

É DUPLICATA se:
- occurred_at dentro de 4h do existente
- Não adiciona informação nova
```

### 6.6 `.claude/skills/trump-tracker/review.md`

```markdown
# Skill: review (Telegram)

Moderação de eventos com score ≥ 8 via Telegram Bot API.
NÃO é um dashboard web — é 100% via Telegram inline keyboards.

## Fluxo
1. classify_agent detecta score ≥ 8
2. telegram_agent envia card formatado para TELEGRAM_CHAT_ID
3. Moderador responde via [✅ Publicar] [✏️ Editar] [❌ Rejeitar]
4. Cloudflare Worker (webhook) recebe callback e atualiza status no banco
5. No próximo run do cron, publish_agent processa os aprovados

## Configuração
TELEGRAM_BOT_TOKEN — via @BotFather
TELEGRAM_CHAT_ID — ID do chat do moderador

## Webhook vs. Polling
SEMPRE webhook em produção. O Cloudflare Worker acorda apenas
quando o moderador responde. Zero custo de compute em idle.
```

### 6.7 `.claude/skills/trump-tracker/publish.md`

```markdown
# Skill: publish

Pega eventos aprovados (status='approved' ou 'approved_manual'),
gera embedding final, insere em events, revalida cache do Next.js.

## Fluxo
1. SELECT raw_articles WHERE status IN ('approved', 'approved_manual')
2. Gera embedding: text-embedding-3-small
3. Gera slug: {ação-kebab}-{YYYY-MM-DD}
4. INSERT events com ON CONFLICT DO NOTHING
5. UPDATE raw_articles SET status='published'
6. POST /api/revalidate para revalidar ISR do Next.js

## Revalidação Next.js no Cloudflare
POST /api/revalidate
Authorization: Bearer {REVALIDATE_SECRET}
Body: {"tags": ["events-feed"]}

## Regras
- NUNCA deletar um evento publicado — marcar como merged_into_id
- Score ≥ 8 só publica se review_status = 'human_approved' ou 'human_edited'
- ON CONFLICT DO NOTHING como proteção contra race conditions
```

### 6.8 `.claude/skills/trump-tracker/backfill.md`

```markdown
# Skill: backfill

Preenche o feed com eventos históricos para o feed não começar vazio.

## Quando usar
- /tracker backfill [dias]
- /tracker backfill --from 2025-01-20 (desde a posse)
- Primeiro setup do sistema

## Estratégia por período

Curto (< 60 dias):
- ingest_agent com lookback_hours expandido
- NewsAPI (30 dias no free tier)
- GDELT (histórico ilimitado)

Médio (60–365 dias):
- GDELT como fonte primária
- Guardian API (histórico completo)
- Wikipedia API como âncora de eventos marcantes

Longo (> 1 ano, mandato 2017–2021):
- GDELT + Guardian + Wikipedia
- Processar em batches de 50/dia
- Dedup threshold mais conservador: 0.95

## Seed mínimo de eventos canônicos
Para o MVP, inserir manualmente os 5–10 eventos mais
marcantes da posse (jan/2025) com fontes primárias linkadas.
```

### 6.9 `agents/ingest_agent.py` (completo)

```python
"""
ingest_agent.py
---------------
Busca notícias sobre Trump nas fontes configuradas, normaliza o formato
e persiste na tabela raw_articles do Supabase com status='pending'.

Fluxo:
  1. Lê config/sources.yml para queries e fontes ativas
  2. Busca em paralelo: NewsAPI, GDELT, Guardian, RSS
  3. Filtra por relevância (sem IA — regex + keywords)
  4. Dedup por URL contra o banco (SELECT em batch antes de INSERT)
  5. Insere novos artigos com status='pending'
  6. Imprime relatório

Uso:
  python agents/ingest_agent.py
  python agents/ingest_agent.py --lookback-hours 48
  python agents/ingest_agent.py --dry-run
  python agents/ingest_agent.py --source gdelt
"""

import argparse
import asyncio
import hashlib
import logging
import os
import sys
from dataclasses import dataclass, field
from datetime import datetime, timedelta, timezone
from typing import Optional
from urllib.parse import urlencode

import feedparser
import httpx
import yaml
from dotenv import load_dotenv
from supabase import create_client, Client

load_dotenv()

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s — %(message)s",
    datefmt="%H:%M:%S",
)
log = logging.getLogger("ingest")

REQUIRED_ENV = ["SUPABASE_URL", "SUPABASE_KEY"]
OPTIONAL_ENV = ["NEWSAPI_KEY", "GUARDIAN_API_KEY"]

RELEVANCE_TERMS = [
    "trump", "white house", "executive order", "president trump",
    "mar-a-lago", "truth social", "trump administration",
]

HIGH_PRIORITY_TERMS = [
    "unprecedented", "first time", "never before", "reversal", "reversed",
    "fires", "fired", "dismissed", "emergency", "tariff", "pardon",
    "invoke", "invokes", "suspend", "overturns", "defies", "blocks",
    "sued", "indicted", "arrested", "admits",
]


@dataclass
class RawArticle:
    url: str
    title: str
    body: str
    source_name: str
    source_tier: int
    published_at: datetime
    raw_query: str
    priority: bool = False

    @property
    def url_hash(self) -> str:
        return hashlib.sha256(self.url.encode()).hexdigest()[:16]

    def to_db_row(self) -> dict:
        return {
            "url": self.url,
            "url_hash": self.url_hash,
            "title": self.title,
            "body": self.body[:2000],
            "source_name": self.source_name,
            "source_tier": self.source_tier,
            "published_at": self.published_at.isoformat(),
            "status": "pending",
            "priority": self.priority,
            "raw_query": self.raw_query,
        }


@dataclass
class IngestReport:
    source_counts: dict = field(default_factory=dict)
    raw_fetched: int = 0
    after_relevance_filter: int = 0
    already_in_db: int = 0
    inserted: int = 0
    errors: list[str] = field(default_factory=list)

    def add_source(self, name: str, count: int):
        self.source_counts[name] = count
        self.raw_fetched += count

    def print(self):
        print("\n" + "─" * 55)
        print("✅  Ingestão concluída")
        print("─" * 55)
        for src, n in self.source_counts.items():
            print(f"   {src:<20} {n:>4} artigos brutos")
        print(f"\n   Total bruto:           {self.raw_fetched:>4}")
        print(f"   Após filtro relevância: {self.after_relevance_filter:>4}")
        print(f"   Já no banco (dedup):   {self.already_in_db:>4}")
        print(f"   ✨ Inseridos (pending): {self.inserted:>4}")
        if self.errors:
            print(f"\n   ⚠️  Erros ({len(self.errors)}):")
            for e in self.errors:
                print(f"      • {e}")
        print(f"\n   Próximo passo: classify_agent.py")
        print("─" * 55 + "\n")


def load_config(path: str = "config/sources.yml") -> dict:
    if not os.path.exists(path):
        log.error(f"Config não encontrada: {path}")
        sys.exit(1)
    with open(path) as f:
        return yaml.safe_load(f)


def check_env():
    missing = [k for k in REQUIRED_ENV if not os.getenv(k)]
    if missing:
        log.error(f"Variáveis de ambiente faltando: {', '.join(missing)}")
        sys.exit(1)
    for k in OPTIONAL_ENV:
        if not os.getenv(k):
            log.warning(f"{k} não definida — fonte correspondente será pulada.")


def is_relevant(title: str) -> bool:
    t = title.lower()
    return any(term in t for term in RELEVANCE_TERMS)


def is_high_priority(title: str) -> bool:
    t = title.lower()
    return any(term in t for term in HIGH_PRIORITY_TERMS)


def parse_datetime(value: str | None) -> datetime:
    if not value:
        return datetime.now(timezone.utc)
    for fmt in (
        "%Y-%m-%dT%H:%M:%SZ",
        "%Y-%m-%dT%H:%M:%S%z",
        "%a, %d %b %Y %H:%M:%S %z",
        "%a, %d %b %Y %H:%M:%S GMT",
        "%Y%m%d%H%M%S",
    ):
        try:
            dt = datetime.strptime(value, fmt)
            if dt.tzinfo is None:
                dt = dt.replace(tzinfo=timezone.utc)
            return dt
        except ValueError:
            continue
    return datetime.now(timezone.utc)


def gdelt_datetime(dt: datetime) -> str:
    return dt.strftime("%Y%m%d%H%M%S")


async def fetch_newsapi(
    client: httpx.AsyncClient,
    queries: list[str],
    since: datetime,
    api_key: str,
) -> list[RawArticle]:
    if not api_key:
        return []
    combined_query = " OR ".join(f'"{q}"' for q in queries[:5])
    params = {
        "q": combined_query,
        "from": since.strftime("%Y-%m-%dT%H:%M:%S"),
        "sortBy": "publishedAt",
        "language": "en",
        "pageSize": 100,
        "apiKey": api_key,
    }
    try:
        r = await client.get("https://newsapi.org/v2/everything", params=params, timeout=20)
        r.raise_for_status()
        data = r.json()
    except httpx.HTTPStatusError as e:
        if e.response.status_code == 429:
            log.warning("NewsAPI: rate limit atingido. Pulando.")
        else:
            log.warning(f"NewsAPI: erro HTTP {e.response.status_code}")
        return []
    except Exception as e:
        log.warning(f"NewsAPI: falha — {e}")
        return []

    articles = []
    for item in data.get("articles", []):
        url = item.get("url", "")
        title = item.get("title", "") or ""
        if not url or not title or url == "[Removed]":
            continue
        articles.append(RawArticle(
            url=url,
            title=title,
            body=(item.get("description") or "") + " " + (item.get("content") or ""),
            source_name=item.get("source", {}).get("name", "NewsAPI"),
            source_tier=2,
            published_at=parse_datetime(item.get("publishedAt")),
            raw_query=combined_query,
            priority=is_high_priority(title),
        ))
    log.info(f"NewsAPI: {len(articles)} artigos")
    return articles


async def fetch_gdelt(
    client: httpx.AsyncClient,
    queries: list[str],
    since: datetime,
) -> list[RawArticle]:
    articles = []
    for query in queries:
        params = {
            "query": f"{query} sourcelang:english",
            "mode": "artlist",
            "maxrecords": 250,
            "format": "json",
            "startdatetime": gdelt_datetime(since),
            "sort": "DateDesc",
        }
        try:
            r = await client.get(
                "https://api.gdeltproject.org/api/v2/doc/doc",
                params=params,
                timeout=30,
            )
            r.raise_for_status()
            data = r.json()
        except httpx.TimeoutException:
            log.warning(f"GDELT: timeout para '{query}'. Pulando.")
            continue
        except Exception as e:
            log.warning(f"GDELT: erro para '{query}' — {e}")
            continue

        for item in data.get("articles", []):
            url = item.get("url", "")
            title = item.get("title", "") or ""
            if not url or not title:
                continue
            articles.append(RawArticle(
                url=url,
                title=title,
                body=item.get("seendate", ""),
                source_name=item.get("domain", "GDELT"),
                source_tier=2,
                published_at=parse_datetime(item.get("seendate")),
                raw_query=query,
                priority=is_high_priority(title),
            ))
        await asyncio.sleep(0.5)

    log.info(f"GDELT: {len(articles)} artigos")
    return articles


async def fetch_guardian(
    client: httpx.AsyncClient,
    queries: list[str],
    since: datetime,
    api_key: str,
) -> list[RawArticle]:
    if not api_key:
        return []
    articles = []
    since_date = since.strftime("%Y-%m-%d")
    for query in queries[:4]:
        params = {
            "q": query,
            "from-date": since_date,
            "order-by": "newest",
            "show-fields": "headline,bodyText,shortUrl",
            "page-size": 50,
            "api-key": api_key,
        }
        try:
            r = await client.get(
                "https://content.guardianapis.com/search",
                params=params,
                timeout=20,
            )
            r.raise_for_status()
            data = r.json()
        except httpx.HTTPStatusError as e:
            if e.response.status_code == 401:
                log.warning("Guardian API: chave inválida. Pulando.")
                return articles
            log.warning(f"Guardian API: erro {e.response.status_code}")
            continue
        except Exception as e:
            log.warning(f"Guardian API: falha — {e}")
            continue

        for item in data.get("response", {}).get("results", []):
            url = item.get("webUrl", "")
            fields = item.get("fields", {})
            title = fields.get("headline") or item.get("webTitle", "")
            if not url or not title:
                continue
            articles.append(RawArticle(
                url=url,
                title=title,
                body=(fields.get("bodyText") or "")[:2000],
                source_name="The Guardian",
                source_tier=2,
                published_at=parse_datetime(item.get("webPublicationDate")),
                raw_query=query,
                priority=is_high_priority(title),
            ))

    log.info(f"Guardian: {len(articles)} artigos")
    return articles


async def fetch_rss(
    client: httpx.AsyncClient,
    feeds: list[dict],
    since: datetime,
) -> list[RawArticle]:
    articles = []
    for feed_config in feeds:
        url = feed_config["url"]
        name = feed_config["name"]
        tier = feed_config.get("tier", 1)
        try:
            r = await client.get(url, timeout=15, follow_redirects=True)
            r.raise_for_status()
            feed = feedparser.parse(r.text)
        except Exception as e:
            log.warning(f"RSS [{name}]: erro — {e}")
            continue

        for entry in feed.entries:
            title = entry.get("title", "") or ""
            link = entry.get("link", "") or ""
            summary = entry.get("summary", "") or ""
            if not link or not title:
                continue
            published_raw = entry.get("published") or entry.get("updated")
            published = parse_datetime(published_raw)
            if published < since:
                continue
            if not is_relevant(title):
                continue
            articles.append(RawArticle(
                url=link,
                title=title,
                body=summary[:2000],
                source_name=name,
                source_tier=tier,
                published_at=published,
                raw_query="rss",
                priority=is_high_priority(title),
            ))

    log.info(f"RSS: {len(articles)} artigos relevantes")
    return articles


def dedup_against_db(
    articles: list[RawArticle],
    supabase: Client,
) -> tuple[list[RawArticle], int]:
    if not articles:
        return [], 0
    url_to_article = {a.url: a for a in articles}
    all_urls = list(url_to_article.keys())
    existing_urls: set[str] = set()

    batch_size = 500
    for i in range(0, len(all_urls), batch_size):
        batch = all_urls[i : i + batch_size]
        try:
            result = (
                supabase.table("raw_articles")
                .select("url")
                .in_("url", batch)
                .execute()
            )
            for row in result.data:
                existing_urls.add(row["url"])
        except Exception as e:
            log.warning(f"Erro ao checar duplicatas: {e}")

    new_articles = [a for a in articles if a.url not in existing_urls]
    duplicates = len(articles) - len(new_articles)
    return new_articles, duplicates


def insert_articles(
    articles: list[RawArticle],
    supabase: Client,
    dry_run: bool = False,
) -> tuple[int, list[str]]:
    if not articles or dry_run:
        if dry_run:
            log.info(f"[DRY RUN] Seriam inseridos: {len(articles)} artigos")
        return 0, []

    rows = [a.to_db_row() for a in articles]
    errors = []
    inserted = 0

    batch_size = 100
    for i in range(0, len(rows), batch_size):
        batch = rows[i : i + batch_size]
        try:
            result = (
                supabase.table("raw_articles")
                .upsert(batch, on_conflict="url", ignore_duplicates=True)
                .execute()
            )
            inserted += len(result.data)
        except Exception as e:
            err = f"Erro ao inserir batch {i//batch_size + 1}: {e}"
            log.error(err)
            errors.append(err)

    return inserted, errors


async def run(
    lookback_hours: int = 4,
    dry_run: bool = False,
    source_filter: Optional[str] = None,
):
    check_env()
    config = load_config()

    supabase: Client = create_client(
        os.environ["SUPABASE_URL"],
        os.environ["SUPABASE_KEY"],
    )

    since = datetime.now(timezone.utc) - timedelta(hours=lookback_hours)
    log.info(f"Buscando artigos desde {since.strftime('%Y-%m-%d %H:%M')} UTC")

    queries_primary = config.get("queries", {}).get("primary", [])
    all_articles: list[RawArticle] = []
    report = IngestReport()

    async with httpx.AsyncClient(
        headers={"User-Agent": "TrumpTracker/1.0 (news aggregator)"},
        follow_redirects=True,
    ) as client:
        tasks = {}

        if not source_filter or source_filter == "newsapi":
            if os.getenv("NEWSAPI_KEY"):
                tasks["newsapi"] = fetch_newsapi(
                    client, queries_primary, since, os.environ["NEWSAPI_KEY"]
                )
        if not source_filter or source_filter == "gdelt":
            tasks["gdelt"] = fetch_gdelt(client, queries_primary, since)
        if not source_filter or source_filter == "guardian":
            if os.getenv("GUARDIAN_API_KEY"):
                tasks["guardian"] = fetch_guardian(
                    client, queries_primary, since, os.environ["GUARDIAN_API_KEY"]
                )
        if not source_filter or source_filter == "rss":
            rss_feeds = config.get("rss_feeds", [])
            if rss_feeds:
                tasks["rss"] = fetch_rss(client, rss_feeds, since)

        results = await asyncio.gather(*tasks.values(), return_exceptions=True)

        for name, result in zip(tasks.keys(), results):
            if isinstance(result, Exception):
                log.error(f"Fonte '{name}' falhou: {result}")
                report.errors.append(f"{name}: {result}")
                report.add_source(name, 0)
            else:
                report.add_source(name, len(result))
                all_articles.extend(result)

    relevant = [a for a in all_articles if is_relevant(a.title)]
    report.after_relevance_filter = len(relevant)

    new_articles, already_in_db = dedup_against_db(relevant, supabase)
    report.already_in_db = already_in_db

    new_articles.sort(key=lambda a: (not a.priority, -a.published_at.timestamp()))

    inserted, errors = insert_articles(new_articles, supabase, dry_run=dry_run)
    report.inserted = inserted
    report.errors.extend(errors)
    report.print()
    return report


def main():
    parser = argparse.ArgumentParser(description="Trump Tracker — Ingest Agent")
    parser.add_argument("--lookback-hours", type=int, default=4)
    parser.add_argument("--dry-run", action="store_true")
    parser.add_argument(
        "--source",
        choices=["newsapi", "gdelt", "guardian", "rss"],
        default=None,
    )
    args = parser.parse_args()
    asyncio.run(run(
        lookback_hours=args.lookback_hours,
        dry_run=args.dry_run,
        source_filter=args.source,
    ))


if __name__ == "__main__":
    main()
```

### 6.10 `config/sources.yml`

```yaml
# config/sources.yml
lookback_hours: 4

queries:
  primary:
    - "Trump executive order"
    - "Trump fires OR fired OR dismissed"
    - "Trump unprecedented OR \"first time\" OR \"never before\""
    - "Trump reversal OR reversed"
    - "Trump tariff emergency"
    - "Trump pardon"
    - "Trump invokes OR invoked"
    - "Trump suspends OR suspended"

  secondary:
    - "White House staff dismissed"
    - "Trump administration policy reversal"
    - "Federal judge blocks Trump"
    - "Trump declares emergency"
    - "Trump claims election"
    - "Trump threatens allies"
    - "Trump NATO"
    - "Trump defy congress"
    - "Trump overturns"

rss_feeds:
  - name: "AP News Politics"
    url: "https://feeds.apnews.com/rss/apf-politics"
    tier: 1
  - name: "AP News Top"
    url: "https://feeds.apnews.com/rss/apf-topnews"
    tier: 1
  - name: "Reuters Politics"
    url: "https://feeds.reuters.com/Reuters/PoliticsNews"
    tier: 1
  - name: "Reuters Top"
    url: "https://feeds.reuters.com/reuters/topNews"
    tier: 1
  - name: "BBC US"
    url: "https://feeds.bbci.co.uk/news/world/us_and_canada/rss.xml"
    tier: 2
  - name: "NPR Politics"
    url: "https://feeds.npr.org/1014/rss.xml"
    tier: 2

sources:
  newsapi:
    enabled: true
    max_results: 100
    tier: 2
  gdelt:
    enabled: true
    max_results_per_query: 250
    tier: 2
  guardian:
    enabled: true
    max_results_per_query: 50
    tier: 2
  rss:
    enabled: true
    tier: 1
```

### 6.11 `requirements.txt`

```txt
# Python 3.12+

# HTTP async
httpx==0.27.0

# RSS parsing
feedparser==6.0.11

# Supabase client
supabase==2.4.0

# Config e env
python-dotenv==1.0.1
pyyaml==6.0.1

# Normalização de texto (slugs)
unidecode==1.3.8

# Próximos agents (descomentar ao implementar):
# anthropic==0.28.0          # classify_agent
# openai==1.30.0             # embeddings (dedup_agent, publish_agent)
# python-telegram-bot==21.0  # telegram_agent
```

---

## 7. Estrutura de Documentação Mintlify

O projeto usa Mintlify como padrão de documentação. A estrutura abaixo deve ser mantida à medida que novos componentes são desenvolvidos.

### `docs/mint.json`

```json
{
  "$schema": "https://mintlify.com/schema.json",
  "name": "Trump Tracker",
  "logo": {
    "light": "/logo/light.svg",
    "dark": "/logo/dark.svg"
  },
  "favicon": "/favicon.svg",
  "colors": {
    "primary": "#DC2626",
    "light": "#EF4444",
    "dark": "#991B1B"
  },
  "topbarLinks": [
    { "name": "GitHub", "url": "https://github.com/seu-usuario/trump-tracker" }
  ],
  "topbarCtaButton": {
    "name": "Ver Feed",
    "url": "https://trumptracker.pages.dev"
  },
  "navigation": [
    {
      "group": "Início",
      "pages": ["docs/briefing", "docs/setup", "docs/architecture"]
    },
    {
      "group": "Pipeline",
      "pages": [
        "docs/pipeline/overview",
        "docs/pipeline/ingest",
        "docs/pipeline/classify",
        "docs/pipeline/dedup",
        "docs/pipeline/telegram",
        "docs/pipeline/publish",
        "docs/pipeline/backfill"
      ]
    },
    {
      "group": "Frontend",
      "pages": [
        "docs/frontend/overview",
        "docs/frontend/feed",
        "docs/frontend/event-page",
        "docs/frontend/og-images"
      ]
    },
    {
      "group": "Infraestrutura",
      "pages": [
        "docs/infra/supabase",
        "docs/infra/cloudflare",
        "docs/infra/github-actions",
        "docs/infra/telegram-webhook"
      ]
    }
  ],
  "footerSocials": {
    "github": "https://github.com/seu-usuario/trump-tracker"
  }
}
```

### Estrutura de arquivos da documentação

```
docs/
├── mint.json
├── briefing.mdx              ← Este arquivo (especificação completa)
├── setup.mdx                 ← Como configurar do zero
├── architecture.mdx          ← Diagrama e decisões de design
│
├── pipeline/
│   ├── overview.mdx          ← Fluxo geral do pipeline
│   ├── ingest.mdx            ← ingest_agent.py
│   ├── classify.mdx          ← classify_agent.py
│   ├── dedup.mdx             ← dedup_agent.py
│   ├── telegram.mdx          ← telegram_agent.py + webhook
│   ├── publish.mdx           ← publish_agent.py
│   └── backfill.mdx          ← backfill_agent.py
│
├── frontend/
│   ├── overview.mdx
│   ├── feed.mdx              ← Infinite scroll, ISR, cursor pagination
│   ├── event-page.mdx        ← Página individual, meta tags
│   └── og-images.mdx        ← Geração dinâmica de OG images
│
└── infra/
    ├── supabase.mdx          ← Schema, RLS, pgvector
    ├── cloudflare.mdx        ← Pages config, adapter next-on-pages
    ├── github-actions.mdx    ← Workflow, secrets, cron
    └── telegram-webhook.mdx  ← Cloudflare Worker, setup do bot
```

---

## 8. Boas Práticas e Regras de Desenvolvimento

### 8.1 Regras gerais de código

```
REGRA-001: Toda persistência de estado vai para o Supabase.
           Proibido escrever dados de pipeline em arquivos locais (.jsonl, .tsv, .json).
           Arquivos locais = apenas configuração (.yml, .env) e código.

REGRA-002: Nenhum agente roda em loop infinito ou polling contínuo.
           GitHub Actions para cron de pipeline.
           Cloudflare Worker (webhook) para callbacks do Telegram.
           Zero processos em idle.

REGRA-003: Toda chamada de API externa tem timeout explícito e tratamento de erro.
           Falha de uma fonte não quebra o pipeline — apenas loga e continua.
           O pipeline termina com sucesso mesmo que 0 artigos sejam inseridos.

REGRA-004: Nenhum evento de score ≥ 8 é publicado sem aprovação humana via Telegram.
           Esta regra é hardcoded — não configurável por .env ou YAML.

REGRA-005: Nenhum registro é deletado do banco.
           Rejected → status='rejected'.
           Merged → merged_into_id preenchido.
           Unpublished → status='unpublished'.
           Toda ação é reversível e auditável.

REGRA-006: Credenciais nunca são commitadas.
           .env está no .gitignore.
           GitHub Actions usa Secrets para todas as keys.
           .env.example contém APENAS os nomes das variáveis, sem valores.

REGRA-007: Cada agente é idempotente.
           Rodar o mesmo agente duas vezes não duplica dados.
           ON CONFLICT DO NOTHING no INSERT de raw_articles e events.
           status checks antes de processar garantem que já-processados são ignorados.

REGRA-008: Tokens de IA são um recurso caro. Usar apenas quando necessário.
           Filtro de keyword ANTES de qualquer chamada ao Claude.
           Truncagem de corpo para 800 chars antes de enviar ao Claude.
           Batching de 5 artigos por chamada ao Claude.
           Anthropic Prompt Caching para o system prompt da classificação.

REGRA-009: Todo código Python segue PEP 8 e usa type hints.
           Dataclasses para modelos de dados.
           Logging estruturado (não print) exceto em relatórios finais.
           Asyncio para todas as operações de I/O.

REGRA-010: Toda feature nova precisa de documentação MDX correspondente no Mintlify
           antes de ser considerada concluída.
```

### 8.2 Regras editoriais do sistema

```
EDITORIAL-001: O sistema classifica fatos, não emite opiniões.
               Headlines descrevem o que aconteceu, não o que significa.

EDITORIAL-002: Toda publicação exige no mínimo 1 fonte de Tier 1 ou 2.
               Twitter/X, blogs e substack não são fontes primárias.

EDITORIAL-003: Score ≤ 3 é automaticamente rejeitado.
               Eventos rotineiros não poluem o feed.

EDITORIAL-004: Dedup é obrigatório antes de qualquer publicação.
               O mesmo episódio com 10 coberturas = 1 card com fontes agrupadas.

EDITORIAL-005: Calibração mensal do sistema de scoring.
               A cada 30 eventos revisados via Telegram, analisar padrões
               de aprovação/rejeição para ajustar os critérios.
```

### 8.3 Regras de infraestrutura

```
INFRA-001: GitHub Actions como única fonte de orquestração do pipeline.
           Não usar servidores EC2, VMs, ou containers sempre-ligados para cron.

INFRA-002: Cloudflare Pages com adapter @cloudflare/next-on-pages.
           NÃO usar Vercel — o hosting é Cloudflare.
           Revalidação via tag (events-feed), não por path.

INFRA-003: Supabase free tier é suficiente para o MVP.
           500MB de storage, 50k req/dia.
           Monitorar usage antes de escalar para Pro.

INFRA-004: Índice ivfflat no pgvector é suficiente até 100k eventos.
           Migrar para HNSW apenas se performance de similarity search degradar.

INFRA-005: Secrets no GitHub Actions são a única forma de passar credenciais.
           Nunca hardcodar API keys, mesmo em branches privadas.
```

---

## 9. Critérios de Aceite

### CA-01 — Ingestão autônoma

- [ ] O cron do GitHub Actions executa a cada 2 horas sem intervenção humana
- [ ] Falha de qualquer fonte individual não impede o pipeline de concluir
- [ ] Artigos duplicados (mesma URL) não são inseridos mais de uma vez
- [ ] O relatório de ingestão imprime contagens corretas por fonte
- [ ] `--dry-run` executa sem nenhuma escrita no banco
- [ ] `--source gdelt` executa apenas a fonte GDELT

### CA-02 — Classificação inteligente

- [ ] Artigos com score ≤ 3 são rejeitados automaticamente (status='rejected')
- [ ] Artigos com score 4–7 seguem para dedup automaticamente
- [ ] Artigos com score ≥ 8 são enviados para o Telegram antes de qualquer publicação
- [ ] A resposta do Claude é sempre JSON válido sem markdown
- [ ] Batching de 5 artigos por chamada está implementado e funcionando
- [ ] Prompt Caching está ativo e reduz custo em runs consecutivos

### CA-03 — Deduplicação semântica

- [ ] Artigos sobre o mesmo episódio (similaridade ≥ 0.92) são mesclados
- [ ] Updates do mesmo episódio (similaridade 0.80–0.91) enriquecem o evento existente sem criar novo card
- [ ] Eventos relacionados mas distintos (< 0.80) são publicados separadamente
- [ ] Nenhum registro é deletado — apenas marcado como merged_into_id

### CA-04 — Moderação via Telegram

- [ ] Card de moderação é enviado para TELEGRAM_CHAT_ID dentro de 10 minutos após classificação
- [ ] Inline keyboard tem 3 opções: Publicar, Editar, Rejeitar
- [ ] Aprovação via Telegram resulta em publicação no próximo run do cron
- [ ] Rejeição via Telegram resulta em status='rejected' no banco
- [ ] O Cloudflare Worker de webhook responde em < 2s ao Telegram
- [ ] Nenhum evento de score ≥ 8 aparece no feed sem review_status = 'human_approved' ou 'human_edited'

### CA-05 — Publicação e feed

- [ ] Eventos aprovados aparecem no feed em < 2h após aprovação (próximo run do cron)
- [ ] Cada evento tem URL canônica única: `/event/{slug}`
- [ ] OG image é gerada dinamicamente por evento (headline + score + categoria)
- [ ] Feed funciona com scroll infinito e cursor pagination
- [ ] Counter de aberrações no header é atualizado a cada publicação
- [ ] Cache do Next.js é revalidado automaticamente após cada run do publish_agent

### CA-06 — Performance e economia

- [ ] Custo total de IA ≤ $2.00/mês em operação normal (20 eventos aberrantes/dia)
- [ ] O pipeline completo (ingest → classify → dedup → publish) termina em < 5 minutos
- [ ] Feed carrega em < 1s no CDN da Cloudflare (LCP < 1s)
- [ ] Nenhuma chamada de IA é feita para artigos que falharam no filtro de keyword

### CA-07 — Operações e manutenibilidade

- [ ] `.env.example` tem todas as variáveis documentadas com descrição
- [ ] `supabase/schema.sql` pode ser executado do zero e cria todas as tabelas e índices
- [ ] Todos os agents aceitam `--dry-run`
- [ ] Logs são suficientes para diagnosticar falha em qualquer etapa sem acesso ao servidor
- [ ] Documentação Mintlify está publicada e acessível para cada componente entregue

---

## 10. Roadmap Atualizado

### Fase 1 — Pipeline autônomo (MVP)

> **Meta:** Sistema rodando de ponta a ponta sem intervenção humana, exceto moderação de score ≥ 8 via Telegram.

| Item | Status | Prioridade |
|------|--------|------------|
| `CLAUDE.md` + skills (7 arquivos) | ✅ Concluído | — |
| `supabase/schema.sql` | ✅ Concluído | — |
| `config/sources.yml` | ✅ Concluído | — |
| `requirements.txt` | ✅ Concluído | — |
| `agents/ingest_agent.py` | ✅ Concluído | — |
| `agents/classify_agent.py` | 🔲 Pendente | P0 |
| `agents/dedup_agent.py` | 🔲 Pendente | P0 |
| `agents/telegram_agent.py` | 🔲 Pendente | P0 |
| `cloudflare/telegram-webhook.js` | 🔲 Pendente | P0 |
| `agents/publish_agent.py` | 🔲 Pendente | P0 |
| `.github/workflows/pipeline.yml` | 🔲 Pendente | P0 |
| `.env.example` | 🔲 Pendente | P0 |

### Fase 2 — Frontend público

> **Meta:** Feed acessível publicamente, otimizado para compartilhamento viral.

| Item | Status | Prioridade |
|------|--------|------------|
| `web/` — Next.js 15 scaffold | 🔲 Pendente | P1 |
| `EventCard.tsx` + `AberrationBadge.tsx` | 🔲 Pendente | P1 |
| Feed com scroll infinito + cursor pagination | 🔲 Pendente | P1 |
| Página individual `/event/[slug]` | 🔲 Pendente | P1 |
| OG images dinâmicas (`/api/og`) | 🔲 Pendente | P1 |
| Filtro por categoria (tabs) | 🔲 Pendente | P1 |
| Counter "X aberrações desde a posse" | 🔲 Pendente | P2 |
| Share button com preview nativo | 🔲 Pendente | P2 |
| Deploy no Cloudflare Pages | 🔲 Pendente | P1 |
| Revalidação ISR via `REVALIDATE_SECRET` | 🔲 Pendente | P1 |

### Fase 3 — Backfill histórico

| Item | Status | Prioridade |
|------|--------|------------|
| `agents/backfill_agent.py` | 🔲 Pendente | P2 |
| Seed de eventos desde 20/jan/2025 | 🔲 Pendente | P2 |
| Seed opcional do mandato 2017–2021 | 🔲 Pendente | P3 |

### Fase 4 — Escala e refinamento

| Item | Status | Prioridade |
|------|--------|------------|
| Calibração automática de score via feedback Telegram | 🔲 Pendente | P3 |
| Dashboard de métricas (share_count, view_count) | 🔲 Pendente | P3 |
| Integração Perplexity API para contexto histórico | 🔲 Pendente | P3 |
| Migração pgvector ivfflat → HNSW (se necessário) | 🔲 Condicional | — |
| Supabase Pro (se ultrapassar free tier) | 🔲 Condicional | — |

---

## 11. Variáveis de Ambiente

```bash
# .env.example
# Copie para .env e preencha os valores.
# NUNCA commite o .env com valores reais.

# ─── Supabase ─────────────────────────────────
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
SUPABASE_KEY=eyJhbGci...

# ─── Anthropic (classificação) ────────────────
ANTHROPIC_API_KEY=sk-ant-api03-...

# ─── OpenAI (embeddings) ──────────────────────
OPENAI_API_KEY=sk-proj-...

# ─── Fontes de notícias ───────────────────────
# NewsAPI — https://newsapi.org (free: 100 req/dia)
NEWSAPI_KEY=

# Guardian — https://open-platform.theguardian.com (free)
GUARDIAN_API_KEY=

# ─── Telegram (moderação) ─────────────────────
# Criar bot via @BotFather no Telegram
TELEGRAM_BOT_TOKEN=

# ID do chat do moderador
# Obter via @userinfobot no Telegram
TELEGRAM_CHAT_ID=

# ─── Frontend / Cloudflare ────────────────────
NEXT_PUBLIC_SITE_URL=https://trumptracker.pages.dev

# String aleatória para autenticar revalidação do Next.js
# Gerar com: openssl rand -hex 32
REVALIDATE_SECRET=

# ─── Opcional ─────────────────────────────────
# Perplexity API para enriquecimento contextual (fase 4)
PERPLEXITY_API_KEY=
```

---

## 12. Estrutura de Repositório

```
trump-tracker/
│
├── CLAUDE.md                          ← Instruções para Claude Code
├── .env                               ← NÃO commitar (gitignored)
├── .env.example                       ← Template documentado
├── .gitignore
├── requirements.txt                   ← Dependências Python
│
├── .claude/
│   └── skills/
│       └── trump-tracker/
│           ├── _shared.md             ← Categorias, rubrica, padrões editoriais
│           ├── ingest.md
│           ├── classify.md
│           ├── dedup.md
│           ├── review.md              ← Moderação via Telegram
│           ├── publish.md
│           └── backfill.md
│
├── agents/
│   ├── ingest_agent.py                ← ✅ Concluído
│   ├── classify_agent.py              ← 🔲 Próximo
│   ├── dedup_agent.py                 ← 🔲 Pendente
│   ├── telegram_agent.py              ← 🔲 Pendente
│   ├── publish_agent.py               ← 🔲 Pendente
│   └── backfill_agent.py              ← 🔲 Fase 3
│
├── cloudflare/
│   └── telegram-webhook.js            ← Cloudflare Worker (webhook Telegram)
│
├── config/
│   ├── sources.yml                    ← ✅ Concluído
│   └── sources.example.yml
│
├── web/                               ← Next.js 15 (Fase 2)
│   ├── app/
│   │   ├── page.tsx                   ← Feed infinito
│   │   ├── event/[slug]/page.tsx      ← Página individual
│   │   └── api/
│   │       ├── events/route.ts        ← Cursor pagination
│   │       ├── revalidate/route.ts    ← ISR revalidation
│   │       └── og/route.tsx           ← OG images dinâmicas
│   └── components/
│       ├── EventCard.tsx
│       ├── AberrationBadge.tsx
│       ├── CategoryFilter.tsx
│       └── ShareButton.tsx
│
├── supabase/
│   └── schema.sql                     ← ✅ Concluído
│
├── docs/                              ← Documentação Mintlify
│   ├── mint.json
│   ├── briefing.mdx                   ← Este arquivo
│   ├── setup.mdx
│   ├── architecture.mdx
│   ├── pipeline/
│   │   ├── overview.mdx
│   │   ├── ingest.mdx
│   │   ├── classify.mdx
│   │   ├── dedup.mdx
│   │   ├── telegram.mdx
│   │   ├── publish.mdx
│   │   └── backfill.mdx
│   ├── frontend/
│   │   ├── overview.mdx
│   │   ├── feed.mdx
│   │   ├── event-page.mdx
│   │   └── og-images.mdx
│   └── infra/
│       ├── supabase.mdx
│       ├── cloudflare.mdx
│       ├── github-actions.mdx
│       └── telegram-webhook.mdx
│
└── .github/
    └── workflows/
        └── pipeline.yml               ← Cron a cada 2h
```

---

## 13. Pendências e Próximos Passos

### Para retomar o desenvolvimento nesta ordem:

**1. `classify_agent.py`** — implementar com:
   - Batching de 5 artigos por chamada ao Claude
   - Anthropic Prompt Caching no system prompt
   - Truncagem do corpo para 800 chars
   - Roteamento: score ≤ 3 → rejected; 4–7 → dedup; ≥ 8 → telegram

**2. `dedup_agent.py`** — implementar com:
   - Geração de embedding via `text-embedding-3-small`
   - Query de similaridade cosine no Supabase (pgvector)
   - Tabela de decisão: 0.92 / 0.80–0.91 / 0.65–0.79 / <0.65
   - Lógica de update vs. duplicata para faixa 0.80–0.91

**3. `telegram_agent.py` + `cloudflare/telegram-webhook.js`** — implementar com:
   - Card formatado com headline, summary, score breakdown e link
   - Inline keyboard: Publicar / Editar / Rejeitar
   - Cloudflare Worker como handler de webhook (não polling)
   - Configurar webhook URL via `setWebhook` da Bot API

**4. `publish_agent.py`** — implementar com:
   - SELECT de aprovados (status IN ('approved', 'approved_manual'))
   - Geração de embedding final e slug
   - INSERT em `events` com ON CONFLICT DO NOTHING
   - POST para `/api/revalidate` após publicação

**5. `.github/workflows/pipeline.yml`** — criar workflow com:
   - Cron `0 */2 * * *`
   - Execução sequencial dos 4 agents
   - Todos os secrets mapeados do GitHub → env
   - `workflow_dispatch` com inputs para `lookback_hours` e `dry_run`

**6. Frontend Next.js** — iniciar pela Fase 2 do roadmap

---

> **Referência de projeto** — Este briefing foi desenvolvido tomando como base arquitetural o projeto [`career-ops`](https://github.com/santifer/career-ops) (Santiago Fer), especificamente o padrão de `CLAUDE.md` + skills por modo + agentes especializados. A principal adaptação foi substituir o modelo de arquivos locais (`.jsonl`, `.tsv`) pelo modelo de banco de dados centralizado (Supabase), adequado para um sistema com cron autônomo em produção.